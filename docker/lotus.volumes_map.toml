# Volume mappings for lotus Docker image
# Format: host_subdirectory = "container_path"
# Host paths will be relative to ~/.foc-localnet/artifacts/docker/volumes/lotus/

[volumes]
# Lotus repository data
data = "/root/.lotus-local-net"
# Genesis files
genesis = "/root/.genesis"
